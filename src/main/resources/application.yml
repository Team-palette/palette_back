server:
  address: localhost
  port: 8088

spring:
  jpa:
    show_sql: true

    hibernate:
      ddl-auto: none
      database-platform: org.hibernate.dialect.MariaDB103Dialect
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy

    properties:
      hibernate:
        default_batch_fetch_size: 500
        format_sql: true
        jdbc:
          time_zone: KST

  datasource:
    url: jdbc:h2:mem:palette?useSSL=false&serverTimezone=KST&useLegacyDatetimeCode=false&characterEncoding=UTF-8
    driverClassName: org.h2.Driver
    username: sa
    password:

logging:
  level:
    org.hibernate.SQL: DEBUG

springdoc:
  version: v1
